import React from 'react'

const ShowProduct = (props) => {
    console.log('ShowProduct', props.productItem);
    return (
        <div>
            ShowProduct
            -----
        </div>
    )
}

export default ShowProduct;